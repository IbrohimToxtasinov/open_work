import 'package:flutter/material.dart';

class WorkerUpdateProfileScreen extends StatelessWidget {
  const WorkerUpdateProfileScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
